export interface AnalyticsMetric { views: number; unique_visitors: number; shares: number; clicks: number; leads: number; conversions: number; view_to_lead_rate: number | null; lead_to_customer_rate: number | null }
export interface AnalyticsDashboard { date_from: string; date_to: string; scope: 'company' | 'employee'; metrics: AnalyticsMetric; trend: Array<{ date: string; views: number; shares: number; clicks: number; leads: number; conversions: number }>; ranking_dimension: string; ranking: Array<{ dimension_id: string; dimension_name: string; views: number; leads: number; conversions: number }>; funnel: Array<{ code: string; name: string; value: number; previous_rate: number | null }>; samples: Array<{ id: string; event_type: string; event_category: string; employee_id: string | null; card_id: string | null; product_id: string | null; lead_id: string | null; customer_id: string | null; channel: string; occurred_at: string }>; definitions: Array<{ code: string; name: string; formula: string; deduplication: string }>; last_updated_at: string | null; filtered_bot_events: number; filtered_internal_events: number }
